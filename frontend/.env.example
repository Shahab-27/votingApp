# API base URL (backend)
VITE_API_BASE_URL=http://localhost:8000/api/candidates

# Cloudinary (unsigned upload for candidate photo & party logo)
# Create an "Upload preset" in Cloudinary Dashboard → Settings → Upload → Add upload preset (unsigned).
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_unsigned_preset_name
